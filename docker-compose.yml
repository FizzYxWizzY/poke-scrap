version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    volumes:
      - ./frontend:/app:Z
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
